package accessmodifier03;

import accessmodifier02.Parent;
//Child2는 accessmodifier3패키지 소속
//Parent는 accessmodifier2패키지 소속으로
//부모자식간 클래스 소속이 다르다.

public class Child2 extends Parent {
	protected int e;//메인과 같은 패키지 소속
//	             (accessmodifier3)이기때문에 메인에서 값을 부를 수 있음. 

	
//	child2의 getB 메서드는 Parent의 b에 접근합니다.
//	getB()와 같이 내부 요소를 조회만 할 수 있고
//	수정은 할 수 없게 제한하는 메서드를 getter라고 합니다.
	public void getB() {
		System.out.println(b);
	}
}
